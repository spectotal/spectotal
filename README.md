# Spectotal Monorepo

Initial foundation for Spectotal with four packages:

- `@spectotal/core`
- `@spectotal/lint`
- `@spectotal/ast-patch`
- `@spectotal/profile-w3c`

## Commands

```bash
pnpm install
pnpm -r typecheck
pnpm -r test
pnpm -r build
```
